var nome=prompt("quanto tepo eu tenho para tomar este suco?");
var contadorDaCorrida = 5;
while (contadorDaCorrida > 0){
    alert("em "+contadorDaCorrida+"s!");
    contadorDaCorrida --;
}
alert("voce perdeu o desafio");
var w;
function iniciar() {
    if(typeof(button) !== "undefined");
        if(typeof(w) == "undefined");
           


function parar() { 
    w.terminate();
    w = undefined;
}
